config = {
	use_home_symbol = true,
	use_root_symbol = true,
	use_short_paths = true,
	use_posix_paths = true,
	use_prompt_newline = false
}

symbols = {
	new_line = "\n",
	arrow = "➜",
	lambda = "λ",
	home = "~",
	root = "/",
	git_dirty = "✗"
}
