config = {
	use_home_symbol = true,
	use_root_symbol = true,
	use_short_paths = true,
	use_posix_paths = true,
	use_prompt_newline = true,
	use_git_dirty_flag = true,
	use_npm_package_name = true,
	use_npm_package_version = true
}

symbols = {
	new_line = "\n",
	arrow = "➜",
	lambda = "λ",
	home = "~",
	root = "/",
	git_dirty = "✗"
}
